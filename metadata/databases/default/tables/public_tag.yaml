table:
  schema: public
  name: tag
select_permissions:
  - role: manager
    permission:
      columns:
        - id
        - name
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - name
      filter: {}
